name: ci

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  gotest:
    name: Test Backend
    runs-on: ubuntu-latest
    steps:
    - name: Invalid yaml spacing
      run: echo hello
