name: ci

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  gotest:
    name: Test Backend
    runs-on: ubuntu-latest
    steps:
    - name: yaml spacing issue
             run: echo hello
